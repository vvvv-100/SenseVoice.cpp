name: Build SenseVoice Vulkan (Win64)

on:
  workflow_dispatch:

jobs:
  build-vulkan:
    runs-on: windows-latest
    defaults:
      run:
        shell: powershell

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: lovemefan/SenseVoice.cpp
          submodules: recursive

      # ---------- 1. 初始化 MSVC 环境 ----------
      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      # ---------- 2. 安装 Ninja ----------
      - name: Install Ninja
        run: choco install ninja -y --no-progress

      # ---------- 3. 用 vcpkg 安装 Vulkan（头文件 + 加载器 + shader编译器） ----------
      - name: Install Vulkan via vcpkg
        run: |
          cd C:\vcpkg
          .\vcpkg.exe install vulkan-headers vulkan-loader shaderc --triplet x64-windows

      # ---------- 4. 配置 CMake（指定 glslc 路径） ----------
      - name: Configure CMake
        run: |
          mkdir build
          cd build
          cmake .. -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DGGML_VULKAN=ON `
            -DGGML_OPENBLAS=OFF `
            -DGGML_NATIVE=OFF `
            -DCMAKE_TOOLCHAIN_FILE="C:/vcpkg/scripts/buildsystems/vcpkg.cmake" `
            -DVulkan_GLSLC_EXECUTABLE="C:/vcpkg/installed/x64-windows/tools/shaderc/glslc.exe"

      # ---------- 5. 编译 ----------
      - name: Build
        run: |
          cd build
          ninja

      # ---------- 6. 打包上传 ----------
      - name: Collect and Upload
        run: |
          mkdir artifact
          copy build\bin\*.exe artifact\
          copy C:\vcpkg\packages\vulkan-loader_x64-windows\bin\vulkan-1.dll artifact\

      - uses: actions/upload-artifact@v4
        with:
          name: sense-voice-vulkan-win64
          path: artifact/
